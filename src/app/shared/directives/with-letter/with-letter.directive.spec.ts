import { WithLetterDirective } from './with-letter.directive';

describe('WithLetterDirective', () => {
  it('should create an instance', () => {
    const directive = new WithLetterDirective();
    expect(directive).toBeTruthy();
  });
});
